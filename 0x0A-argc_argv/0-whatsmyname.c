#include <stdio.h>

/**
 * main - prints it name, follow by new line
 * @argc: argument to count
 * @argv: argument s
 *
 * Return: 0.
 */
int main(int argc, char **argv)
{
	(void)argc;

	printf("%s\n", argv[0]);
	return (0);
}
