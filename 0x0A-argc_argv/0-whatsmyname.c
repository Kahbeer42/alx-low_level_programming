#include <stdio.h>

/**
 * main - prints it name, follow by new line
 * @agrc: argument to count
 * @argv: argument s
 *
 * Return: 0.
 */
int main(int argc, char **argv)
{
	(void)argc;

	printf("%s\n", argv[]);
	return (0);
}
