#include <stdio.h>

/**
 * main - assign a random number to variable n each time it executed
 * and print the last digit of number stored on variable n
 * Return: Always 0
 */
int main(void)
{
int n;

for (n = 0; n < 10; n++)
{
printf("%d" ' n);
}
printf("\n");
return (0)
}
